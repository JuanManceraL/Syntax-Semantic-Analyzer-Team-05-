# C languaje lexer analyzer
This program identifies and classifies into tokens the lexemes found in its input. It can identify keywords, identifiers, punctuations, operators, constans, literals and directives.
